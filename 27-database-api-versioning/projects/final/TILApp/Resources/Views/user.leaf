#extend("base"):
  #export("content"):
    <h1>#(user.name)</h1>
    <h2>#(user.username)
      #if(user.twitterURL):
      - @#(user.twitterURL)
      #endif
    </h2>
    
    #extend("acronymsTable")
  #endexport
#endextend